# Caps journald's total disk usage and retention, since news-alert.service logs go
# through journald rather than a plain file logrotate could manage. Deployed to
# /etc/systemd/journald.conf.d/news-alert.conf on the server.
[Journal]
SystemMaxUse=200M
MaxRetentionSec=30day
